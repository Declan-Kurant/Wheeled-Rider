<%= form_for @vehicle do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :"vehicle URL" %>
  <%= f.text_field :img_url %>

  <%= f.submit %>

<% end %>

<!-- Nice job using _forms to keep things dry -->
